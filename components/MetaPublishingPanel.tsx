"use client";
import {useEffect,useState} from "react";
import {CalendarClock,Facebook,Instagram,ShieldCheck} from "lucide-react";
type Status={configured:boolean;checks:{appId:boolean;appSecret:boolean;encryptionKey:boolean};callbackUrl:string;requiredPermissions:string[]};
export default function MetaPublishingPanel(){const[data,setData]=useState<Status|null>(null);useEffect(()=>{fetch("/api/meta/status",{cache:"no-store"}).then(r=>r.json()).then(setData).catch(()=>setData(null))},[]);return <section className="panel meta-panel"><div className="meta-heading"><div><div className="eyebrow">PHASE 4 · META PUBLISHING</div><h2><Instagram size={24}/><Facebook size={24}/> Connect, approve and schedule</h2><p>Publishing stays approval-first. Nothing posts until you explicitly approve it.</p></div><span className={`readiness ${data?.configured?"ready":"setup"}`}>{data?.configured?<><ShieldCheck size={16}/> Ready to connect</>:"Setup required"}</span></div>{data&&!data.configured&&<div className="meta-setup"><h3>Meta connection prerequisites</h3><div className="check-grid"><span className={data.checks.appId?"done":""}>META_APP_ID</span><span className={data.checks.appSecret?"done":""}>META_APP_SECRET</span><span className={data.checks.encryptionKey?"done":""}>TOKEN ENCRYPTION KEY</span></div><label>OAuth callback URL<code>{data.callbackUrl}</code></label><p className="permission-note">Permissions: {data.requiredPermissions.join(", ")}</p></div>}{data?.configured&&<div className="meta-ready"><CalendarClock size={20}/><div><strong>Credentials detected</strong><p>The next step will authorize your Facebook Page and linked Instagram Professional account.</p></div><button type="button" disabled>Connect Meta Account — next step</button></div>}</section>}
