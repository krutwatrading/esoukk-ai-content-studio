"use client";
import{useEffect,useState,type ReactNode}from"react";import{Facebook,Ghost,Instagram,Music2,Search,ShieldCheck,Unplug,Youtube}from"lucide-react";
type Channel={id:string;name:string;configured:boolean;connected:boolean;accountName:string|null;callbackUrl:string;keys:string[]};type Status={channels:Channel[]};
const icons:Record<string,ReactNode>={instagram:<Instagram/>,facebook:<Facebook/>,tiktok:<Music2/>,google:<><Search/><Youtube/></>,snapchat:<Ghost/>};
export default function MetaPublishingPanel(){const[data,setData]=useState<Status|null>(null),[expanded,setExpanded]=useState<string|null>(null);useEffect(()=>{fetch("/api/meta/status",{cache:"no-store"}).then(response=>response.json()).then(setData).catch(()=>setData(null))},[]);function openQueue(){document.getElementById("content-review-queue")?.scrollIntoView({behavior:"smooth",block:"start"})}return <section className="panel meta-panel channel-hub" id="meta-publishing"><div className="meta-heading"><div><div className="eyebrow">PHASE 4 · OMNICHANNEL PUBLISHING</div><h2>Connect, approve and publish everywhere</h2><p>One approval-first control centre for Instagram, Facebook, TikTok, Google/YouTube and Snapchat.</p></div><span className="readiness ready"><ShieldCheck size={16}/>{data?.channels.filter(item=>item.connected).length||0} connected</span></div><div className="channel-grid">{(data?.channels||[]).map(channel=><article key={channel.id} className={`channel-card ${channel.connected?"connected":channel.configured?"configured":"setup"}`}><header><div className="channel-icon">{icons[channel.id]}</div><span className="channel-state">{channel.connected?"Connected":channel.configured?"Credentials ready":"Setup required"}</span></header><h3>{channel.name}</h3><p>{channel.connected?`@${channel.accountName||"Business account"}`:channel.configured?"Authorize the business account to activate publishing.":"Add official developer credentials in Vercel before authorization."}</p><div className="channel-actions">{channel.id==="instagram"&&channel.configured?<a href="/api/meta/connect">{channel.connected?"Reconnect":"Connect"}</a>:channel.connected?<button type="button" onClick={openQueue}>Open Queue</button>:<button type="button" onClick={()=>setExpanded(expanded===channel.id?null:channel.id)}>{expanded===channel.id?"Hide setup":"Setup details"}</button>}</div>{expanded===channel.id&&<div className="channel-setup"><strong>Vercel environment variables</strong>{channel.keys.map(key=><code key={key}>{key}</code>)}<strong>OAuth callback</strong><code>{channel.callbackUrl}</code><small>The official OAuth and publishing endpoint will be activated after these developer credentials are supplied.</small></div>}</article>)}</div><div className="channel-policy"><Unplug size={18}/><div><strong>Approval-first across every channel</strong><p>Setup channels remain visible for planning, but cannot publish until their developer credentials, OAuth authorization and required platform review are complete.</p></div><button type="button" className="ui-action" onClick={openQueue}>Open Review Queue</button></div></section>}
