"use client";import{useEffect,useRef}from"react";import{CampaignCopy,ProductData}from"@/lib/types";import{brand}from"@/lib/brand";
const wrap=(c:CanvasRenderingContext2D,t:string,w:number)=>{const a:string[]=[];let l="";for(const x of t.split(/\s+/)){const q=l?`${l} ${x}`:x;if(c.measureText(q).width>w&&l){a.push(l);l=x}else l=q}if(l)a.push(l);return a.slice(0,4)};
export default function CreativeCanvas({label,width,height,product,campaign,background}:{label:string;width:number;height:number;product:ProductData;campaign:CampaignCopy;background?:string}){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const canvas=ref.current;if(!canvas)return;canvas.width=width;canvas.height=height;const c=canvas.getContext("2d");if(!c)return;(async()=>{c.fillStyle=brand.palette.ivory;c.fillRect(0,0,width,height);if(background){const bg=new Image();bg.src=background;await bg.decode().catch(()=>{});if(bg.naturalWidth){const s=Math.max(width/bg.naturalWidth,height/bg.naturalHeight);c.globalAlpha=.82;c.drawImage(bg,(width-bg.naturalWidth*s)/2,(height-bg.naturalHeight*s)/2,bg.naturalWidth*s,bg.naturalHeight*s);c.globalAlpha=1}}if(product.images[0]){const i=new Image();i.crossOrigin="anonymous";i.src=product.images[0];await i.decode().catch(()=>{});if(i.naturalWidth){const area=height*.60,s=Math.min(width*.82/i.naturalWidth,area/i.naturalHeight),dw=i.naturalWidth*s,dh=i.naturalHeight*s;c.shadowColor="rgba(24,24,24,.16)";c.shadowBlur=25;c.drawImage(i,(width-dw)/2,height*.06+(area-dh)/2,dw,dh);c.shadowBlur=0}}const p=width*.07,y=height*.70;c.fillStyle=brand.palette.charcoal;c.font=`600 ${width*.031}px Arial`;c.fillText("K A V I A",p,y);c.font=`700 ${width*.057}px Georgia`;const lines=wrap(c,campaign.headline.toUpperCase(),width-p*2);lines.forEach((l,n)=>c.fillText(l,p,y+width*.075+n*width*.063));c.font=`500 ${width*.026}px Arial`;c.fillStyle="rgba(24,24,24,.76)";const sub=wrap(c,campaign.subheadline,width-p*2),sy=y+width*.075+lines.length*width*.063+width*.02;sub.slice(0,2).forEach((l,n)=>c.fillText(l,p,sy+n*width*.035));const by=Math.min(height-width*.12,sy+sub.length*width*.035+width*.04);c.fillStyle=brand.palette.charcoal;c.beginPath();c.roundRect(p,by,width*.31,width*.075,width*.04);c.fill();c.fillStyle="#fff";c.font=`700 ${width*.023}px Arial`;c.fillText(campaign.cta,p+width*.035,by+width*.047)})()},[width,height,product,campaign,background]);const download=()=>{const a=document.createElement("a");a.download=`${product.handle}-${label.toLowerCase().replace(/\s+/g,"-")}.png`;a.href=ref.current!.toDataURL("image/png");a.click()};return <div className="creative-card"><canvas ref={ref}/><div className="creative-tools"><strong>{label} · {width}×{height}</strong><button onClick={download}>Download PNG</button></div></div>}
