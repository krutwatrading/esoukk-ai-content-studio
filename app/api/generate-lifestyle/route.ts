import OpenAI from "openai"; import {NextRequest,NextResponse} from "next/server";
export async function POST(req:NextRequest){try{if(!process.env.OPENAI_API_KEY)throw new Error("OPENAI_API_KEY is required for lifestyle image generation.");const b=await req.json();const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const styles:any={"minimal-luxury":"minimal luxury studio, warm ivory background, soft light",editorial:"high-fashion editorial architecture",resort:"premium UAE resort, natural sunlight",performance:"modern premium fitness studio"};const result=await client.images.generate({model:process.env.OPENAI_IMAGE_MODEL||"gpt-image-1",size:"1024x1536",quality:"medium",prompt:`Create a premium vertical social campaign background for women's fashion titled ${b.productTitle}. ${styles[b.style]||styles["minimal-luxury"]}. Leave negative space for headline and CTA. No text, logos, trademarks or celebrities.`});const image=result.data?.[0]?.b64_json;if(!image)throw new Error("No image returned.");return NextResponse.json({dataUrl:`data:image/png;base64,${image}`});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Unable to generate image."},{status:400})}}
