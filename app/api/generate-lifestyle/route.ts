import OpenAI from "openai";
import {NextRequest,NextResponse} from "next/server";
const styles:Record<string,string>={luxury:"champagne gold luxury set, refined depth, soft directional light",minimal:"clean ivory minimal set, generous negative space",studio:"crisp premium catalogue studio, realistic softbox lighting",summer:"sunlit summer setting, warm fresh palette", "uae-lifestyle":"modern premium UAE lifestyle setting, elegant contemporary architecture",gym:"premium modern fitness studio, confident active energy",casual:"relaxed elevated everyday interior, natural window light",resort:"premium UAE resort atmosphere, natural sunlight", "premium-black":"dramatic black studio with restrained gold highlights",editorial:"high-fashion editorial architecture, magazine composition"};
export async function POST(req:NextRequest){try{if(!process.env.OPENAI_API_KEY)throw new Error("OPENAI_API_KEY is required for lifestyle image generation.");const body=await req.json(),client=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const result=await client.images.generate({model:process.env.OPENAI_IMAGE_MODEL||"gpt-image-1",size:"1024x1536",quality:"medium",prompt:`Create a premium vertical social campaign background for women's fashion titled ${body.productTitle}. ${styles[body.style]||styles.luxury}. Leave negative space for headline and CTA. No text, logos, trademarks or celebrities.`});const image=result.data?.[0]?.b64_json;if(!image)throw new Error("No image returned.");return NextResponse.json({dataUrl:`data:image/png;base64,${image}`});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unable to generate image."},{status:400})}}
